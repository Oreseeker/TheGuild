import { Encounter } from '@/types/encounters';
import { gnarlroot } from '@/data/amirdrassil/bosses/gnarlroot';

export const normalGnarlroot: Encounter = {
  name: gnarlroot.name,
  img: gnarlroot.image,
  defeated: true,
  datetime: null,
  warcraftLogsLink: null,
  participants: null,
};
