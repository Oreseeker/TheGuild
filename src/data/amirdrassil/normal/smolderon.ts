import { Encounter } from '@/types/encounters';
import smolderon from '@/data/amirdrassil/bosses/smolderon';

export const normalSmolderon: Encounter = {
  name: smolderon.name,
  img: smolderon.image,
  defeated: true,
  datetime: null,
  warcraftLogsLink: null,
  participants: null,
};
