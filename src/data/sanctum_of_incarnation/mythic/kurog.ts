import { kurog } from '@/data/sanctum_of_incarnation/bosses/kurog';
import { Encounter } from '@/types/encounters';

export const mythicKurog: Encounter = {
  name: kurog.name,
  img: kurog.image,
  defeated: false,
};
